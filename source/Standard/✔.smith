use Bool
use Fun

‣‣ of condition:Bool message
	condition.if
		∙ mess
			`(message == null) ? 'Check failed.' : message()`
		✘ mess

‣‣ fails fun:Fun
	eg
		fails
			✘ 「whoops」

		fails
			fails
				# doesn't fail

	✘.try
		fun.of
		✘ 「did not fail」


