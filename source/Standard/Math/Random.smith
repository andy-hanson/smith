doc
	Generates random numbers.

use! Global

static!

‣‣ in-0-1:ℝ
	doc
		Random fraction.

	Global@Math.random

‣‣new of:ℝ min:ℝ max:ℝ
	doc
		Random number from (inclusive) min to (exclusive) max.
	in
		✔ (min.≤ max)

	∙ size
		max.- min
	∙ offset
		in-0-1.× size
	min.+ offset

‣‣ int:ℕ min:ℕ max:ℕ
	doc
		Random integer from (inclusive) min to (exclusive) max.
	eg
		∙ x
			int 3 8
		✔ (x.≥ 3)
		✔ (x.< 8)

	(of min max).round-down

‣‣ bool:Bool
	in-0-1.< 0.5

