# Something with 'next'

‣ stop
	✘ 'StopIteration

‣ each do:Fun
	`
		try {
			while (true)
				_do(this.next())
		}
		catch (error) {
			if (error.message == 'StopIteration')
				return null;
			else
				throw error;
		}
