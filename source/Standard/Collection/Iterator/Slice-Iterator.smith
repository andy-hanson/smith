doc
	Users should have fast 'size' and 'of'.
	Holes are not allowed; array must be defined at every index up to size.

super Iterator

data |array:Array !index:ℕ end:ℕ
	in
		index.< end
	()

‣ next
	(index.< end).?
		◆.returning ((array) index)
			index! _increment
	|
		stop
