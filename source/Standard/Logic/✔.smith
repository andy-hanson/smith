static!

\ I know this won't override anything, but ‣‣ relies on ✔.of.
‣‣new-ok of condition:Bool ...args
	doc
		TODO

	`
		// not using condition.¬ to save on stack trace
		if (!condition)
			throw new global.Error((args.length > 0) ? args[0]() : 'Check failed.');

