doc
	Control flow methods.

$$ returning value do-after:Fun
	doc
		Calls do-after on the value, then returns the value.
	eg

		!.runs |run
			!= 'returned
				Control.returning 'returned
					run.run!

	do-after.run! value
	value
