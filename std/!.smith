=+TODO
	`throw new Error("TODO")`

=+loop |fun
	`
	function() {
		while (true) {
			var result = body();
			if (Option.Some['subsumes?'](body))
				return body.value;
		}
	}();
	`

=+while |condition do
	loop
		condition.of.?
			do.of
			None
		|
			Some do.of

=+do-while |do condition
	loop
		(Some do.of).filter condition.of
